#include <stdio.h>

int main()
{
     
   int num, i, fac = 1;
   
   printf("Enter a positive integer: ");
   scanf("%d", &num);

  for(i=1; i<=num; i++) {
      
      fac = fac * i;
      
  }

printf("The factorial of the given %d is: %d", num, fac);

 return 0;
 
}
